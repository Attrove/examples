# User token (starts with sk_). Get this from provisioning a user via the admin API.
# This is NOT your attrove_ API key — it's the per-user token returned by admin.users.create().
ATTROVE_USER_TOKEN=sk_your_user_token_here
ATTROVE_USER_ID=your-user-uuid-here

# Optional: send digest via email (requires Resend account)
# RESEND_API_KEY=re_your_resend_key
# SEND_TO=you@example.com
