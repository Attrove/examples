# User secret key (starts with sk_). Get this from provisioning a user via the admin API.
# This is NOT your partner client_id/client_secret — it's the per-user key returned by admin.users.create().
ATTROVE_SECRET_KEY=sk_your_secret_key_here
ATTROVE_USER_ID=your-user-uuid-here

# Optional: send digest via email (requires Resend account)
# RESEND_API_KEY=re_your_resend_key
# SEND_TO=you@example.com
